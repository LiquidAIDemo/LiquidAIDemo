{
  "scripts": {
    "start": "npm install && concurrently \"cd frontend && npm start\" \"cd backend && npm start\"",
    "test": "cd frontend && npm test"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
